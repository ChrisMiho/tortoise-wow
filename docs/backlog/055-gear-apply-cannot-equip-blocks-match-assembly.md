---
status: implemented
risk: medium
area: tournament/gear
depends-on: 031-gear-tier-armour-weapon-split.md
---

# `gear-apply` cannot dress half the roster, so no match can start

**Problem:** `scripts/tournament/gear-apply.sh` leaves 9 of 20 bots undressed —
the console reports `cannot_equip` per item — so `gear-audit.sh` returns
`complete=5/10` for one team and `6/10` for the other, and `match-run.sh` aborts
before assembly. No tournament match can be run on this host. The finding was
recorded in `docs/playerbots/BG-AI-ANALYSIS.md` §4.5 and deferred to artifact
031, but 031 is scoped to splitting a tier into armour and weapon subsets and
says nothing about `cannot_equip` — so the blocker has a pointer that only looks
like an owner. This artifact is that owner.

**Suspected cause / area:** the provisional tier files generated by artifact 021
appear to select items without checking class/race/level equip requirements
against `tw_world.item_template`, so a tier hands a class an item it cannot wear.
Either the generator's filter or the `tournament equip` handler's requirement
check is the wrong side of the fix — **determine which before writing code**; do
not assume the generator.

**Acceptance criteria:**

- For both shipped teams, `gear-apply.sh` followed by `gear-audit.sh` reports
  `complete=10/10` with zero `cannot_equip` lines.
- `match-run.sh <alliance> <horde>` gets past assembly and reaches
  `tournament start`.
- A regression test under `tests/tournament/` proves a tier containing an item
  the target class cannot equip is rejected **at generation time with a named
  reason**, not silently at equip time.

**Notes:**

- `tw_world.item_template` is **snake_case** on this server — `inventory_type`,
  `item_level`, `required_level`. The CamelCase names in older plans fail with
  `Unknown column`.
- Needs a running world and the `tw_world` database to re-derive tiers. Both are
  available on this host.
- Scope stops at "the match starts". Whether the bots then *play* is artifacts
  046–054, not this one — do not chase bot inaction here.
- `wsg_mysql` discards stderr, so a failed query is indistinguishable from "no
  rows matched". Re-run through a bare `docker exec ... mysql` before concluding
  anything from an empty result.

**Base:** cm-main

**Branch:** backlog/gear-apply-cannot-equip-blocks-match-assembly

**Summary:** Determined the side of the fix before writing code, as the artifact demanded: the generator was NOT at fault. Regenerating all 12 tier files against the live `tw_world` produced output byte-identical to the shipped files, and every item in them passes a class-level equip judgement — so the picks were always class-legal. The failures came from the equip side. `cannot_equip(8)` is `EQUIP_ERR_NO_REQUIRED_PROFICIENCY`: measured over every character row in `tw_char`, no character of any class on this world holds `SKILL_PLATE_MAIL`, no hunter or shaman holds `SKILL_MAIL`, and a bot's weapon skills are whatever `PlayerbotFactory::SetRandomSkill` last set (`InitSkills` grants the armour ones only from level 40 and never re-runs on a bot levelled afterwards) — so a level-60 bot warrior literally cannot wear plate. `cannot_equip(17)` is `EQUIP_ERR_CANT_CARRY_MORE_OF_THIS` on a `max_count > 0` item the bot already wears, and `gear-apply.sh` re-dresses every bot on every run by design, so a unique pick applied exactly once and failed forever after. `HandleTournamentEquipCommand` now (a) grants the proficiency the item demands before calling `CanEquipNewItem`, bounded by a new `TournamentClassMayTrainSkill` table to skills the bot's own class could have trained and only where it holds none at all (weapons at 5×level, armour at 1, mirroring `PlayerbotFactory`), emitting a separate `equip player=<n> granted_proficiency=<skill> value=<v>` record so no existing parsed line changes shape; and (b) destroys the bot's existing copies of a unique item before the equip check. `max_count` is deliberately still not filtered in the generator — 12846 Argent Dawn Commission is the only white trinket on this world, and excluding uniques empties the trinket slot for every class and aborts generation outright (measured). For the third criterion, `itemdb_equip_fault` in `scripts/tournament/lib/itemdb.sh` judges one entry against one class and names the requirement it fails (`no_armor_proficiency(subclass=4)`, `no_weapon_proficiency(subclass=6)`, `class_restricted(...)`, `required_level(70)`, `race_restricted`, `required_skill`, `required_spell`, `required_honor_rank`, `required_reputation_faction`, `deprecated_flag`, `not_obtainable_flag`, `not_equippable(inventory_type=0)`, `no_such_item`); `gear-generate.sh` runs it over every pick before the file is written, and a new `--check [<class> <role>]` mode re-judges files that already exist — the only way a hand-curated tier ever gets judged. New `tests/tournament/gear-generate.test.sh` (15 assertions, docker stubbed, no database) pins the named-reason rejections; all ten `tests/tournament/*.test.sh` files pass. `docs/playerbots/TOURNAMENT-GEAR.md` and the stale `gear-apply.sh` header comment were corrected — both told the reader to look `InventoryResult` up in `SharedDefines.h`, where it is not declared, and both blamed the tier file for code 8. Files touched: `src/game/Commands/TournamentCommands.cpp`, `scripts/tournament/lib/itemdb.sh`, `scripts/tournament/gear-generate.sh`, `scripts/tournament/gear-apply.sh`, `docs/playerbots/TOURNAMENT-GEAR.md`, `tests/tournament/gear-generate.test.sh`.

**In-game check:** Criterion 3 is fully verified here and needs nothing further: `wsl -d Ubuntu -- bash -lc 'cd /mnt/c/Coding/tortoise-wow/tortoise-wow && bash tests/tournament/gear-generate.test.sh'` prints `15 passed, 0 failed`, and all ten `tests/tournament/*.test.sh` pass. `bash scripts/tournament/gear-generate.sh` and `bash scripts/tournament/gear-generate.sh --check` both exit 0 against the live `tw_world` with the db container alone (no server image, no build), the latter printing `ok <class>-<role>` twelve times.

Criteria 1 and 2 CANNOT be confirmed on this host right now, and this is expected rather than a failure: the only server image present is the rollback anchor, which predates the `tournament equip` change, so running `gear-apply.sh` against it still returns `cannot_equip(8)`. That proves nothing about this code. The check below must run against a build that includes this branch — the backlog-batch compile gate.

Scriptable, no human eyes needed (a later batch step can run all of it):
1. Bring the stack up on the batch image, then `./scripts/tournament/roster.sh login stormwind-sentinels` and the same for `orgrimmar-warsong` — both handlers resolve bots through ObjectAccessor, so an offline bot answers `player_not_online` and nothing is written.
2. `./scripts/tournament/gear-apply.sh team stormwind-sentinels`, then the same for `orgrimmar-warsong`. PASS = exit 0, every bot line reads `failed=0`, and `grep -c cannot_equip` over the run output is 0. Expect new `TOURNAMENT equip player=<name> granted_proficiency=<skill> value=<n>` lines — those are the fix working, not an error; `granted_proficiency=293` (plate) on warriors and paladins and `413` (mail) on hunters and shamans is exactly what the measurement predicts.
3. `./scripts/tournament/gear-audit.sh stormwind-sentinels` must print `GEAR-AUDIT stormwind-sentinels complete=10/10 worstMissing=0` and exit 0; same for `orgrimmar-warsong`.
4. Run step 2 a SECOND time without recreating the bots. This is the `cannot_equip(17)` regression and it is the one an operator would otherwise miss: before the fix the first apply passed and every later one failed on the trinket. It must still report `failed=0` and `complete=10/10`.
5. `./scripts/tournament/match-run.sh stormwind-sentinels orgrimmar-warsong` must get past the gear gate and reach `tournament start` — grep the run's `match.log` for a `TOURNAMENT start` line and for the absence of the gate's abort. Scope stops there; whether the bots then play is artifacts 046–054.

Needs a human in the client (one login, two minutes): join the instance as a spectator and look at a warrior bot from `stormwind-sentinels` and a mage bot from the same team. The warrior should be visibly in plate with a weapon drawn and the mage in cloth — the point of the tournament gear path is that both teams wear the same grade of kit, and a bot standing there in its underwear with a filled `character_inventory` row is the one failure the audit cannot see. Also confirm the mage is NOT wearing plate: the proficiency grant is bounded by class, and a mage in plate would mean `TournamentClassMayTrainSkill` is wrong.

**Minor findings:**
- scripts/tournament/gear-generate.sh: `itemdb_equip_fault`'s deliberate third exit code (2 = the query failed, nothing concluded) is collapsed by both callers — `generate_one` prints "the base tier contains an item this class cannot equip" and `check_one` just sets rc=1 — so a database outage is reported to the operator as a bad tier file, the exact conflation the surrounding comments say must never happen.
- src/game/Commands/TournamentCommands.cpp: The `MaxCount > 0` cleanup destroys the bot's existing copies of a unique item before `CanEquipNewItem` is consulted, so if the equip then fails for any other reason (level, a race/class restriction in a hand-curated tier) the bot ends up with an empty slot where the previous code left the old item in place.
- src/game/Commands/TournamentCommands.cpp: Fist weapons are granted SKILL_FIST_WEAPONS so CanUseItem passes and the item equips, but combat rolls read the item's GetProficiencySkill() which is SKILL_UNARMED for subclass 13 (Item.cpp:658-661, Object.cpp:3653, Player.cpp:22076) — a skill this code never grants, so a bot handed a fist weapon can equip it and then swing at whatever unarmed value it happens to have, which is exactly the missing-all-match outcome the 5*level grant was written to prevent.
- src/game/Commands/TournamentCommands.cpp: The new MaxCount>0 branch destroys every copy of the item (equipped, bags and bank) before CanEquipNewItem is consulted, so any pick that fails the subsequent check — level, class, or a prototype that Item::CreateItem cannot build — leaves the bot stripped of the item it was already wearing and reports only cannot_equip/equip_failed, with no path back to the previous tier's kit.
